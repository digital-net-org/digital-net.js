import { UserActions as User } from './UserActions';
import { NavigationActions as Navigation } from './NavigationActions';

export const AppActions = Object.assign(
    {},
    {
        User,
        Navigation,
    }
);
